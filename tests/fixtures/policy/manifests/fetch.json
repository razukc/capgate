{
  "name": "@modelcontextprotocol/server-fetch",
  "version": "0.6.2",
  "serverCapabilities": [],
  "tools": [
    {
      "name": "fetch",
      "description": "Retrieve web content and convert HTML to markdown",
      "capabilities": [
        "net:connect:*",
        "assert:fetch.block_rfc1918:outbound requests to private ranges must be rejected by the egress proxy"
      ]
    }
  ]
}
